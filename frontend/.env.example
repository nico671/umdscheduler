# Frontend runtime environment template
# Copy this file to frontend/.env and set the backend API origin.

# Must be a full http(s) URL without a trailing slash.
VITE_API_BASE_URL=http://localhost:8000
